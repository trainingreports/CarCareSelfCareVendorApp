import { LineGraphConfig } from '../../types';
declare const LineChart: ({ data, height, width, hasLine, lineWidth, lineColor, isBezier, hasShadow, hasDots, dotColor, dotSize, style, baseConfig }: LineGraphConfig) => any;
export default LineChart;
